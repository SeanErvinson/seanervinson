---
import Main from './Main.astro';
import { Image } from 'astro:assets';
import type { MarkdownLayoutProps } from 'astro';

type Props = MarkdownLayoutProps<{
  title: string;
  subtitle: string;
  poster: string;
}>;

const { title, subtitle, poster } = Astro.props.frontmatter;
---

<Main title="Sean Ong | Blog">
  <div class="container">
    <header
      class="relative w-full h-52 bg-gray-800 flex items-center justify-center mt-4 mb-8"
    >
      <Image
        src={poster}
        alt="Poster image"
        width={500}
        height={500}
        class="absolute inset-0 w-full h-full object-cover opacity-50"
      />
      <div class="relative z-10 text-center">
        <h1 class="text-4xl mb-8 font-bold text-white dark:text-grey-900">
          {title}
        </h1>
        <h2 class="text-2xl text-gray-300 dark:text-grey-600">{subtitle}</h2>
      </div>
    </header>

    <!-- <slot /> -->
    <article
      class="w-full prose dark:prose-invert
prose-h1:font-bold prose-h1:text-xl
prose-a:text-blue-600 prose-p:text-justify prose-img:rounded-xl
prose-headings:underline"
    >
      <slot />
    </article>
  </div>
</Main>
